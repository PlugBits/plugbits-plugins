{
  "name": "my-plugin",
  "private": true,
  "scripts": {
    "clean": "rimraf dist ship",
    "build": "node -e \"console.log('build skipped')\"",
    "lint": "eslint . || exit 0",
    "pack:init": "node -e \"console.log('template pack:init skipped')\"",
    "pack": "node -e \"console.log('template pack skipped')\""
  },
   "devDependencies": {
  },
  "engines": {
    "node": ">=18"
  }
}
